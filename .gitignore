logs
*.log
build/Release
node_modules/
dist/
.vite/

.env
.env.local
.env.*.local

# Source corpora for the RAG index. Kept locally only — the vectors live in
# Vectorize, so these files are just build inputs. starwars.json is 66MB, which
# GitHub warns about above 50MB and blocks outright at 100MB.
starwars.json
dune.json

# Wrangler's local state and cache.
.wrangler/

# Generated embedding files, uploaded to Vectorize and then disposable.
# Tens of megabytes, and reproducible with `npm run index`.
vectors-*.ndjson

# Corpus extraction tooling — kept locally for reference, not part of the
# deployed project.
scripts/extract-wiki.mjs

.DS_Store
Thumbs.db
